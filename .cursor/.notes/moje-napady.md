custom mode vyvojar => agent zhodnoti jaky model je pro dany ukol nejoptimalnejsi a rekne doporucuji model x ale default by byl sonnet

custom mode mentor => vysvetluje lidsky co se deje da se s nim bavit neni to vyvojar ale snazi se abych to chapal

---

**Pozn. (2026-08-25, archivní):** Od restrukturalizace (`99d7361`) už neexistují Custom Mode skilly `vyvojar`/`mentor` z `.cursor/skills/`. Obě myšlenky výše žijí dál jako pravidla `@vyvojar.mdc` a `@mentor.mdc` v `.cursor/rules/` — princip (self-assessment modelu u Vývojáře, lidské vysvětlování u Mentora) zůstal zachován, jen se vyvolávají jinak.
